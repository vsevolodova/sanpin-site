# sanpin-site

Лендинг сервиса видеоаналитики для автоматического контроля соблюдения санитарных норм (СанПиН) на пищевых производствах, в комбинатах питания, ресторанах, кафе и других объектах общепита.

Сайт: [sanpin.ssl-team.com](https://sanpin.ssl-team.com/)
![Главный экран](images/head.png)

---

## О проекте

Система в реальном времени анализирует поток с камер видеонаблюдения и фиксирует нарушения санитарных режимов — мытьё рук, использование СИЗ, состояние рабочих зон и т. п. Лендинг презентует продукт для B2B-аудитории: владельцев производств, директоров комбинатов питания, заведующих столовыми.

Главная цель страницы — продать программное обеспечение: коротко и выразительно объяснить, что именно делает продукт, какие задачи решает и какой экономический эффект даёт бизнесу.

## Что внутри

- Готовая посадочная страница на чистом HTML/CSS/JS — без сборщиков и зависимостей.
- Адаптивная вёрстка под десктоп, планшет и мобильные устройства.
- SEO-оптимизация: мета-теги, Open Graph, `robots.txt`, канонический URL, фавиконы, `og-cover`.
- Скрипт генерации Open Graph обложки.
- Скрипт генерации фавикона.
- Черновики контента и бренд-гайд в `data/` и `docs/`.
- Готовая сборка `publish/` для деплоя на статический хостинг.

## Структура папок

```
.
├── index.html          # Главная страница лендинга
├── politika.html       # Политика конфиденциальности
├── claude.md           # Системный файл агента
├── .gitignore
├── assets/             # Статика
│   ├── css/            # Стили
│   ├── js/             # Скрипты
│   └── img/            # Изображения, фавиконы, og-cover
├── data/               # Исходные данные и шаблоны текстов
├── docs/               # Бренд-гайд, контент, дизайн-система
├── scripts/            # Утилиты сборки (favicon, og-cover)
├── publish/            # Готовая сборка для деплоя
└── tmp/                # Временные артефакты (в .gitignore)
```

## Стек

- **HTML5** — каркас страниц.
- **CSS3** — стилизация, адаптивность, анимации.
- **Vanilla JavaScript** — интерактив на странице.
- **Python 3** — скрипты генерации ассетов (`scripts/`).

Внешних JS-библиотек и пакетных менеджеров в репозитории нет.

## Дизайн-система

Основные принципы (полный гайд — в `docs/design.md` и `docs/2026-06-14_brand-guide_minimal.md`):

- **Палитра:** глубокий графит / тёмно-синий фон, акцент — холодный cyan / electric blue, текст — белый и светло-серый.
- **Типографика:** Manrope. Заголовки 52–72 px, основной текст 18–20 px с комфортным межстрочным интервалом.
- **Сетка:** ширина контента рассчитана под десктоп 1920×1080, плавно масштабируется до мобильных.
- **Графика:** смысловой центр слайда, контрастный фон с overlay для читаемости.
- **Иконки:** только смысловые inline-SVG.

## Запуск локально

Проект статический, поэтому достаточно открыть `index.html` в браузере или поднять любой статический сервер.

```bash
# Вариант 1 — Python
python -m http.server 8000

# Вариант 2 — Node.js (если установлен)
npx serve .
```

После запуска сайт будет доступен на `http://localhost:8000`.

## Сборка ассетов

В `scripts/` лежат вспомогательные утилиты:

- `build_favicon_svg.py` — генерация SVG-фавикона.
- `build_og_cover.py` — генерация Open Graph обложки.

Запуск:

```bash
python scripts/build_favicon_svg.py
python scripts/build_og_cover.py
```

Готовые ассеты сохраняются в `assets/img/`.

## Публикация

Финальная сборка лежит в `publish/` (HTML + ассеты + `robots.txt`). Папку можно загружать на любой статический хостинг:

- Nginx/Apache — просто скопировать файлы.
- GitHub Pages — указать источником папку `publish/` или корень с редиректом.
- Cloudflare Pages, Netlify, Vercel — деплой через drag-n-drop или CLI.

В корне репозитория также лежит архив `sanpin-site-publish.zip` (сборка одним файлом) — он исключён из git, чтобы не раздувать историю.

## Контент и редактура

- `data/product.md` — краткое описание продукта.
- `data/product.template.md` — шаблон для заполнения описания.
- `data/Колпино - кейс.txt` — кейс внедрения.
- `data/Какустроена система.txt` — описание архитектуры и принципа работы.
- `docs/landing-content.md` — текст лендинга по блокам.
- `docs/brand-guide_minimal.md` — бренд-бук.
- `docs/design.md` — дизайн-система.

Перед публикацией изменений на боевом домене проверяйте, что все плейсхолдеры из `product.template.md` заменены на реальные значения.

## SEO

- `<title>`, `<meta description>`, `og:*` теги — заполнены в `index.html`.
- `og-cover` генерируется скриптом `scripts/build_og_cover.py`.
- `robots.txt` лежит в `publish/`.
- Канонический URL: `https://sanpin.ssl-team.com/`.

## Лицензия

Проект разработан для внутреннего использования компании. Условия распространения уточняйте у владельца репозитория.

## Контакты

Вопросы по проекту: владелец репозитория — `vsevolodova` на GitHub.
